#include <stdio.h>
#include <stdlib.h>
#include "physics.h"

char** createCoordinateSpace(length, width){
  char** origin = malloc(sizeof(char*)*width);
  int c;
  for(c=0;c<width;c++){
    origin[c] = malloc(sizeof(char)*length)l
  }
  return origin;
}
