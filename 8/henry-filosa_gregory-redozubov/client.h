#ifndef client
#define client

Int process();
/*======== Int process() ==========
  Inputs:  char[] command
  Returns: Boolean
  Processes command and breaks into components. Returns 0 if invalid command.
  ====================*/

struct package():
/*======== Void package() ==========
  Inputs: char[]
  Returns: struct *
  ====================*/

#endif
